services:
  postgres:
    env_file: .env
    image: postgres:18-alpine
    container_name: conf_api_db
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - '5432:5432'

volumes:
  postgres_data:
